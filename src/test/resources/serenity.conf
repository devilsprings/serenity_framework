headless.mode = false
serenity {
    take.screenshots = FOR_EACH_ACTION
}

# Chrome options can be defined using the chrome.switches property

chrome.switches = """--start-maximized"""

environments {
    default {
    url = "https://demo.anhtester.com/"}
}
